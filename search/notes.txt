


# initial state into q, with cost
    # total cost = current number of steps taken from initial state + heuristic cost
# --- looping
# heapify queue
# heappop, check if goal state reached, return moves if reached
# if reached, return moves
# else
# add the popped move into prev moves
# put children into queue with cost and appendedmoves